import { writable } from "svelte/store"

export const user = writable(null)

export const user_email = writable(null)
export const user_name = writable(null)
export const user_admin = writable(null)
export const user_picture = writable(null)
export const user_color = writable(null)

